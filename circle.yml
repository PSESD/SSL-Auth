machine:
  node:
    version: 0.10.45

dependencies:
  override:
    - cd ./src && npm install

deployment:
  development:
    branch: development
    heroku:
      appname: srx-services-ssl-auth-dev
  test:
    branch: test
    heroku:
      appname: srx-services-ssl-auth-test
  production:
    branch: production
    heroku:
      appname: srx-services-ssl-auth
