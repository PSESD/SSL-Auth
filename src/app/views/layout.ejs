<!DOCTYPE html>
<html lang="en">
<head>
    <% include templates/header.ejs %>
</head>
<body>
<div class="container-fluid">
    <% if(typeof errors === "object" && errors.length !== undefined && errors.length>0) { %>
    <div class="row">
        <div class="col-md-12">
            <div id="error-container">
                <div id="error-container">
                    <div class="alert alert-danger alert-dismissible" role="alert"><button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">×</span><span class="sr-only"><%= __('general.close') %></span></button><%= errors.join("<br>") %></div>
                </div>
            </div>
        </div>
    </div>
    <% } %>
    <div class="row ng-scope">
        <%- body %>
    </div>

</div>
<% include templates/footer.ejs %>

</body>
</html>